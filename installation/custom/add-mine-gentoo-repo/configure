#!/bin/sh
set -eu

REPO_NAME="mine-gentoo"

cd /var/db/repos
git clone "https://github.com/ivanp7/${REPO_NAME}.git"
chown -R portage:portage "/var/db/repos/${REPO_NAME}"

cat << _EOF_ > "/etc/portage/repos.conf/${REPO_NAME}.conf"
[$REPO_NAME]
location = /var/db/repos/${REPO_NAME}
_EOF_

