#!/bin/sh

cd -- "$(dirname -- "$0")"
./check && exit

command -v crontab > /dev/null || exit 1

pacman --needed -S man-db man-pages || exit 1
command -v mandb > /dev/null || exit 1
crontab -l | { cat; echo "@daily /usr/bin/mandb"; } | crontab -
mandb

